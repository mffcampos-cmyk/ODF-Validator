# Runtime
lxml>=5.0
openpyxl>=3.1
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1
python-multipart>=0.0.9
pyyaml>=6.0
# Upstream source sync (odf_validator/sources). Was a test-only dependency
# until the app itself started making HTTP requests.
httpx>=0.27
# PDF DD -> Markdown (pinned: pre-1.0, and its Markdown shape is what
# dd_parser's line regexes match against). abi3 wheels: win_amd64, macOS, manylinux.
pdf-inspector==0.2.6
# .docx Data Dictionaries, and the fallback when pdf-inspector cannot
# process a PDF (or has no wheel for the running platform).
markitdown[docx]>=0.0.1a3
# Tests
pytest>=8.0
